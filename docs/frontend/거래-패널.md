# 거래 패널 (SCR-I003)

> 사용자가 조각을 구매할 때 수량을 선택하고 결제 정보를 확인하는 모달 또는 패널 화면입니다.

**관련 UI 페이지**: SCR-I003 거래 패널

---

## 디자인 캡처

### 기본 화면
![거래 패널 기본 화면](assets/kMX40.png)

---

## 개요

- **라우트**: `/channels/:id/purchase` 또는 모달 (TBD)
- **사용자 목표**: 원하는 수량의 조각을 선택하고 구매 정보 확인
- **인증 필요**: 예

---

## 레이아웃

### 구조
- **상단 고정 헤더**: 
  - 좌측: 닫기/뒤로가기
  - 중앙: "조각 구매" 타이틀
  - 우측: X 닫기 버튼
- **스크롤 가능한 바디 영역**:
  - 상품 정보 카드
  - 수량 선택 카드
  - 가격 정보 카드
  - 면책조항 (disclaimer)
- **하단 고정 CTA**: "결제하기" 버튼

### 뷰포트
- **폭**: 375px (모바일)
- **높이**: 1076px (전체, 바디는 스크롤)

### 그리드 & 간격
- **헤더 높이**: 44px (상태바) + 56px (탑 헤더)
- **바디 패딩**: 20px
- **섹션 간격**: 20px
- **CTA 높이**: 96px

---

## 컴포넌트

### 1. 상태 바
- **높이**: 44px
- **배경색**: `$bg-secondary` (#F7F8FA)

### 2. 헤더
- **높이**: 56px
- **배경색**: `$bg-secondary`
- **콘텐츠**:
  - 좌측: 닫기 아이콘 (rect 24x24px, #CCCCCC)
  - 중앙: "조각 구매" 텍스트 (Pretendard Variable, 16px, bold, fill: `$text-primary`)
  - 우측: X 아이콘 (lucide icon, 24x24px, `$text-primary`)
- **배치**: space-between

### 3. 바디 (Body)

#### 3-1. 상품 정보 카드 (상단)
- **배경색**: `$bg-primary` (#ffffff)
- **보더**: 1px `$border`
- **모서리**: radius-lg (12px)
- **콘텐츠**: 상품 이미지, 채널명, 가격 등
- <!-- TODO: 정확한 상품 정보 구성 확인 -->

#### 3-2. 수량 선택 카드
- **배경색**: `$surface` (#ffffff)
- **보더**: 1px `$border`
- **제목**: "구매 수량" (Pretendard Variable, 14px, bold, `$text-primary`)
- **구성 요소**:
  - **Stepper**: 증감 버튼 + 수량 입력 필드
  - **Quick Select 행**: "빠른 선택" 버튼들 (1, 5, 10, 20 등 미리 정의된 수량)
  - **구분선**: 1px `$border`
  - **추가 선택 항목**: (예: 가격 계산 결과)
- **패딩**: 20px
- **갭**: 16px (섹션 간)

#### 3-3. 가격 정보 카드
- **배경색**: `$surface`
- **보더**: 1px `$border`
- **구성**:
  - 단가
  - 수량 × 단가 = 합계
  - 할인 (있을 경우)
  - 최종 합계

#### 3-4. 면책조항 (Disclaimer)
- **배경색**: `$bg-secondary`
- **텍스트**: 작은 폰트, `$text-tertiary` 색상
- **내용**: 구매 관련 주의사항

### 4. 하단 CTA
- **높이**: 96px
- **배경색**: `$bg-primary`
- **상단 보더**: 1px `$border`
- **버튼**: `btn/defult` 스타일
- **텍스트**: "25,500원 결제하기" (또는 동적으로 계산된 가격)

---

## 상태(States)

| 상태 | 트리거 | 시각적 변화 |
|---|---|---|
| **기본** | 패널 오픈 | 기본 수량 1개 표시 |
| **수량 선택** | Stepper 또는 Quick Select 클릭 | 수량 증감, 가격 실시간 업데이트 |
| **로딩** | 결제 버튼 클릭 | <!-- TODO: 로딩 상태 디자인 --> |
| **에러** | 구매 실패 | <!-- TODO: 에러 메시지 표시 방식 --> |

---

## 인터랙션 & 화면 흐름

```
["조각 구매" 버튼 클릭 (채널 상세)] → [SCR-I003 거래 패널 오픈]
    ↓
[수량 선택] 
  - Stepper로 증감
  - 또는 Quick Select 버튼 클릭
    ↓
[가격 동적 계산 및 표시]
    ↓
["결제하기" 버튼 클릭] → [결제 진행]
    ↓
[결제 성공] → [SCR-I005 구매 완료 페이지]
    ↓
[닫기(X) 버튼 클릭] → [패널 닫기, 이전 화면으로]
```

---

## 디자인 토큰

### 색상
| 토큰 | 값 | 용도 |
|---|---|---|
| `$bg-primary` | #FFFFFF | 메인 배경, 헤더 아래 |
| `$bg-secondary` | #F7F8FA | 상태바, 헤더, 면책조항 배경 |
| `$surface` | #FFFFFF | 카드 배경 |
| `$border` | #EAECEF | 보더 라인 |
| `$text-primary` | #1F1F1F | 제목, 주요 텍스트 |
| `$text-tertiary` | #9CA3AF | 면책조항 텍스트 |
| `$primary` | #FF5252 | 결제하기 버튼 |

### 타이포그래피
| 역할 | 폰트 | 크기 | 굵기 |
|---|---|---|---|
| 헤더 | Pretendard Variable | 16px | 700 (bold) |
| 카드 제목 | Pretendard Variable | 14px | 700 (bold) |
| 본문 | Pretendard Variable | 14px | 400 (regular) |
| 면책조항 | Pretendard Variable | 11px | 400 (regular) |

### 간격
| 토큰 | 값 | 용도 |
|---|---|---|
| 기본 패딩 | 20px | 카드 & 섹션 |
| 섹션 간격 | 20px | 카드 간 간격 |
| 카드 갭 | 16px | 카드 내부 요소 간 |

---

## 개발자 참고사항

### 수량 선택
- **Stepper**: 증감 버튼과 입력 필드로 구성
- **Quick Select**: 미리 정의된 수량 버튼 (1, 5, 10, 20 등)
  - <!-- TODO: Quick Select 옵션 값 확인 -->
- 최소/최대 수량 제한 있는지 확인 필요

### 가격 계산
- 단가 × 선택 수량 = 합계
- 할인 로직 있는지 확인 (수량 기반 할인 등)
- 수수료, 세금 등 추가 항목 있는지 확인

### 결제 플로우
- "결제하기" 클릭 후 결제 게이트웨이로 이동하는지, 다음 단계로 진행하는지 확인
- <!-- TODO: 결제 수단 선택 화면 필요 여부 확인 -->

### 모달 vs 페이지
- 현재 디자인상 모달처럼 보이지만, 실제로는 새로운 페이지인지 모달인지 확인 필요
- 닫기 버튼 클릭 시 동작 (이전 상태 유지 여부)

### 면책조항
- 현재 텍스트 미포함, 백엔드에서 동적으로 제공되는지 확인
